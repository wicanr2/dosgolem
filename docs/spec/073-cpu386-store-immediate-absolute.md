# 073 — 386 絕對位址立即 dword 寫入

狀態：**CONFORMED**  
日期：2026-09-06  
前置：[`RE 052`](../re/052-fd2-post-environment-runtime-list.md)

- 擴充無前綴 `C7 /0`、`mod=0 r/m=5`，依序讀取 DS 絕對位址與 32 位元立即值。
- 寫入失敗不得部分提交；其他新增形狀維持失敗即關閉。
- 固定 FD2 在 `0x46931` 把 `dword_541A0` 清為零，結束此 runtime 初始化函式。

2026-09-06：絕對寫入與固定函式結尾測試通過；探針抵達下一個獨立入口
`0x46123`。
