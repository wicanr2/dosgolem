# 043 — FD2 第二個回呼 x87 控制字保存路徑

狀態：**CONFORMED**  
日期：2026-09-06  
前置：[`RE 035`](../re/035-fd2-second-callback-x87-store.md)

- 不增加新的 CPU 指令語意；重用既有 `MOV`、`SUB`、`PUSH`、`FNINIT` 與
  `FNSTCW` 實作，證明它們可在固定 FD2 狀態連續互通。
- 由 LE 入口自然執行，不得直接設定 EIP、回呼記錄或 x87 狀態。
- 於 `0x460EF` 驗證 `[0x527F5]=0`、EAX=0、ESP=`0x55690`、堆疊控制字與
  CPU 控制字均為 `0x037F`。
- 不替未知全域變數命名；後續 `pop eax` 與分支仍維持失敗即關閉。

驗收包含獨立固定雜湊整合測試與命令列探針收據。

2026-09-06：固定雜湊整合測試、探針與全套 Go 回歸通過；探針於 336 步抵達
`0x460EF`，ESP=`0x55690`、EAX=0，堆疊控制字為 `0x037F`。
