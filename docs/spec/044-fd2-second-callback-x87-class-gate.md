# 044 — FD2 第二個回呼 x87 控制字分類閘門

狀態：**CONFORMED**  
日期：2026-09-06  
前置：[`RE 036`](../re/036-fd2-second-callback-x87-class-gate.md)

- 不增加新 CPU 指令；重用既有 `POP`、暫存器位元組 `MOV`、累加器位元組
  `CMP` 與短程 `JNZ`。
- 由 LE 入口自然執行到 `0x460F6`，不得注入 EIP、控制字或旗標。
- 驗證 EAX=`0x0303`、ESP=`0x55694`、ZF=1，且第二筆回呼記錄仍未被標記。
- `0x460A2` 呼叫及其消費結果不屬於本切片；未支援行為繼續失敗即關閉。

驗收包含獨立固定雜湊整合測試與命令列探針收據。

2026-09-06：固定雜湊整合測試、探針與全套 Go 回歸通過；探針於 340 步抵達
`0x460F6`，EAX=`0x0303`、ESP=`0x55694`、ZF=1。
