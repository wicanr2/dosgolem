# 048 — FD2 第二回呼分類結果保存

狀態：**CONFORMED**  
日期：2026-09-06  
前置：[`RE 040`](../re/040-fd2-second-callback-class-result.md)

- 不新增 CPU 指令，重用既有暫存器位元組 `MOV`、絕對位址 `CMP`、`JNZ` 與
  絕對位址位元組 `MOV`。
- 從固定 FD2 的 LE 入口自然執行至 `0x46112`，不得直接注入第二回呼狀態。
- 驗證 `[0x52830]=0`、BL=3、`[0x527F4]=3`、`[0x527F5]=3`，第二筆回呼記錄
  在返回分派器前仍為 0。
- 不命名未知全域；回呼的 `POP`、`RET` 與後續記錄標記不屬於本切片。

驗收包含獨立固定雜湊整合測試與命令列探針收據。

2026-09-06：固定雜湊整合測試、探針與全套 Go 回歸通過；探針於 372 步抵達
`0x46112`，BL=3，`[0x527F4]=3`、`[0x527F5]=3`。
