# 046 — FD2 控制字基準跳轉落點

狀態：**CONFORMED**  
日期：2026-09-06  
前置：[`RE 038`](../re/038-fd2-control-baseline-dispatch.md)

- 不新增 CPU 指令；重用既有 32 位元近程相對 `JMP`。
- 由固定 FD2 的 LE 入口自然抵達 `0x4CBD0`，不得直接注入 EIP。
- 驗證 EAX=`0x127F`、ESP=`0x55690`，並保留第二回呼尚未返回的堆疊狀態。
- 落點語意尚未證實，本切片不得替 `0x4CBD0` 命名或假設其返回值。

驗收包含獨立固定雜湊整合測試與命令列探針收據。

2026-09-06：固定雜湊整合測試與探針通過；探針於 345 步抵達 `0x4CBD0`，
EAX=`0x127F`、ESP=`0x55690`。
