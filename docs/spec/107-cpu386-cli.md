# 107 — 386 CLI

狀態：**CONFORMED**
日期：2026-09-06
前置：[`RE 067`](../re/067-fd2-ail-interrupt-setup-entry.md)

- 支援無 prefix 的 opcode `FA`（`CLI`）。
- 清除 dosgolem EFLAGS 的 IF，其他旗標與所有暫存器保持不變。
- 本階段沒有真實硬體中斷排程；這項能力只保存被 AIL 程式碼觀測及稍後
  `POPF` 恢復的建模狀態，不宣稱逐週期中斷一致。
- operand-size、segment override 與 repeat prefix 維持失敗即關閉（fail-closed）。
- 固定 FD2 在 `0x3E931` 執行 `CLI`，前一指令已把原旗標壓棧。

驗收：單元測試確認只清除 IF；固定雜湊 FD2 必須由 LE entry 自然執行至
`0x3E932`，確認 IF=0。

驗收收據（2026-09-06）：`TestClearInterruptFlag` 與固定原版
`TestFD2DisablesInterruptsForAILSetupWhenProvided` 通過；後者自然執行至
`0x3E932` 並確認 IF=0。同一原版唯讀掛載下 `go test ./... -count=1`
全數通過。
