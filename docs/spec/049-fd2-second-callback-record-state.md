# 049 — FD2 第二筆回呼記錄完成狀態

狀態：**CONFORMED**  
日期：2026-09-06  
前置：[`RE 041`](../re/041-fd2-second-callback-record-state.md)、[`spec 040`](040-cpu386-record-status-write.md)

- 不新增 CPU 指令，重用既有 `POP`、`RET` 與 `C6 03 02`。
- 由固定 FD2 的 LE 入口自然執行，並以第二次抵達 `0x45DD6` 區分第一及第二
  回呼，禁止直接注入 EIP 或記錄狀態。
- 驗證第一、第二筆記錄的 status 均為 2，第二筆其餘五個 byte 保持不變，
  EBX=`0x539C8`、ESP=`0x5569C`。
- 下一輪重掃與第三筆記錄選擇不屬於本切片。

驗收包含獨立固定雜湊整合測試與命令列探針收據。

2026-09-06：固定雜湊整合測試、探針及全套 Go 回歸通過；第二次於 375 步
抵達 `0x45DD6`，第二筆記錄 status=2、EBX=`0x539C8`、ESP=`0x5569C`。
