# 041 — FD2 第二筆回呼記錄狀態回寫

日期：2026-09-06  
證據等級：**已證實**（固定雜湊、第二回呼返回與記憶體差異）  
輸入：`FD2.EXE`，大小 `357074`，MD5 `b97caf2239a27a896069d03549d96e1e`，
SHA-256 `222b7d067ad4450eb9c5f6e6bce1797d54bb050417ba39ced6067f8039f28c4f`  
工具：dosgolem `feat/fd2-parity`，基線提交 `d8a8d37`  
位址空間：dosgolem 載入 LE 映像後的 32 位元線性位址

第二回呼於 `0x46112` 恢復 EBX 並返回分派器的既有回寫路徑：

```text
0x46112  5B        pop ebx
0x46113  C3        ret
0x45DD3  C6 03 02  mov byte ptr [ebx],2
0x45DD6  EB C6     jmp 0x45D9E
```

返回後 EBX=`0x539C8`。執行回寫後，第二筆記錄由
`00 02 D5 60 04 00` 變為 `02 02 D5 60 04 00`，其餘五個 byte 不變；第一筆
記錄保持 `02 01 CC CB 03 00`。這沿用 RE 032 已證實的同一分派器狀態語意，
不重新解讀或另創記錄格式。
