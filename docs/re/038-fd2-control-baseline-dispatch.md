# 038 — FD2 控制字基準跳入共用路徑

日期：2026-09-06  
證據等級：**已證實**（固定雜湊原始位元組與自然執行前置狀態）  
輸入：`FD2.EXE`，大小 `357074`，MD5 `b97caf2239a27a896069d03549d96e1e`，
SHA-256 `222b7d067ad4450eb9c5f6e6bce1797d54bb050417ba39ced6067f8039f28c4f`  
工具：dosgolem `feat/fd2-parity`，基線提交 `c7baf06`  
位址空間：dosgolem 載入 LE 映像後的 32 位元線性位址

第二回呼的控制字基準讀取後，原始指令為：

```text
0x460BF  0F B7 05 1C390500  movzx eax,word ptr [0x5391C]
0x460C6  E9 056B0000        jmp 0x4CBD0
```

跳轉位移以 `0x460CB + 0x6B05` 計算為 `0x4CBD0`。本切片只證實控制流落點，
不以目的位址推測該共用路徑的高層名稱；其 caller／consumer 關係須由落點原始
指令及返回路徑另行確認。
