# 037 — FD2 第二個啟動回呼讀取控制字基準

日期：2026-09-06  
證據等級：**已證實**（固定雜湊重定位映像、原始位元組與自然執行前置狀態）  
輸入：`FD2.EXE`，大小 `357074`，MD5 `b97caf2239a27a896069d03549d96e1e`，
SHA-256 `222b7d067ad4450eb9c5f6e6bce1797d54bb050417ba39ced6067f8039f28c4f`  
工具：dosgolem `feat/fd2-parity`，基線提交 `5dfac90`  
位址空間：dosgolem 載入 LE 映像後的 32 位元線性位址

由 `0x460F6` 呼叫 `0x460A2` 後，固定狀態 `[0x527F5]=0` 使 `JZ` 跳過兩筆
全域寫入，抵達：

```text
0x460A2  80 3D F5270500 00  cmp byte ptr [0x527F5],0
0x460A9  74 14              jz 0x460BF
...
0x460BF  0F B7 05 1C390500  movzx eax,word ptr [0x5391C]
```

固定重定位映像的 `[0x5391C]` 為 `0x127F`。本切片只證實該讀取及零擴充結果；
`0x5391C` 的完整高層角色仍為**未知**。「它是 x87 控制字分類基準」依相鄰流程
列為**強推論**。
