# 081 — FD2 AIL 比例換算門檻

日期：2026-09-06
證據等級：函式、參數、常數、分支與算式為**已證實**
輸入：`FD2.EXE`，大小 `357074`，MD5 `b97caf2239a27a896069d03549d96e1e`，
SHA-256 `222b7d067ad4450eb9c5f6e6bce1797d54bb050417ba39ced6067f8039f28c4f`
工具：IDA Pro 9.4.0.260610，映像 `ida-pro-9.4-idapython:locked-v1`
位址空間：IDA 載入 LE 映像後的 32 位線性位址

`sub_3E894`（`0x3E894..0x3E8C7`）由 `sub_3E8C7` 在 `0x3E909` 呼叫。
它先令 EAX=0，於 `0x3E89F` 將第一個 stack 參數與 `0xD68D` 比較；
`0x3E8A6 jnb` 在參數大於等於門檻時跳過換算。若小於，則於
`0x3E8A8..0x3E8B7` 計算 `arg×0x2710/0x20BC`，再將結果傳給
`sub_3E864`。

固定原版目前路徑的參數為 `0xD68D`，因此比較結果 ZF=1 並採跳躍分支。
本切片只要求無前綴 `81 /7`、base+disp8 的 dword 與 imm32 比較；
不把此常數命名為未證實的取樣率或硬體時鐘。

一次性 IDA 報告 SHA-256：
`d32026c17e603b5d5df606e05ebf6e4818a5cdafd3d8cfbe441db3f4009cb711`。
